# .readthedocs.yml

version: 2

build:
  image: latest

python:
  version: 3.7
  install:
     - method: setuptools
       path: .

conda:
  environment: docs/environment.yml

conda:
  file: docs/environment.yml
